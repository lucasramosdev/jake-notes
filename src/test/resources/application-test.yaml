spring:
  application:
    name: jake-notes
  datasource:
    url: "jdbc:h2:mem:test"
    username: sa
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect